name = "Rameez"  # Strings are Immutable which means you cannot change them by running functions on them

nameshort = name[0:3]  #start from index 0 all the way till 3 (excluding 3)

print(nameshort)

character1 = name[1]
print(character1)